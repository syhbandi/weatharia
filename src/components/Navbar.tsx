const Navbar = () => {
  return (
    <nav className="fixed top-0 left-0 w-full">
      <div className="container mx-auto py-5 flex items-center justify-between">
        <h2 className="font-semibold text-white text-2xl">Weatharia°</h2>
      </div>
    </nav>
  );
};

export default Navbar;
